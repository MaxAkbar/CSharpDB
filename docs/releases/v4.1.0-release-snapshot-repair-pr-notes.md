## Summary

Repair the version 4.1.0 release qualification failure by aligning the package
and canonical compatibility metadata with 4.1.0, correcting feature-version and
ADO.NET collection claims, regenerating the public matrix, and adding the
required immutable release snapshot.

The release snapshot is intentionally created in a second commit. It records
the preceding qualified commit so the tagged-release gate can prove that no
implementation, test, schema, or canonical-manifest changes occurred after
qualification.

## Type of Change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Documentation update
- [x] Refactor / maintenance
- [ ] Tests only

## Related Issues

Follow-up to the failed version 4.1.0 Release workflow.

## Testing

- [x] `dotnet build CSharpDB.slnx -c Release --no-restore`
- [x] Focused `SqlCompatibilityManifestTests` executed
- [x] Compatibility matrix generation and stale-output check executed
- [x] Documentation schema, proof, internal-link, navigation, and sitemap checks executed
- [x] Local tagged-release qualification executed against the repaired branch

## Checklist

- [x] I followed the project style and conventions.
- [x] Existing validation covers this metadata-only repair; product behavior did not change.
- [x] Release-gate behavior did not change, so its existing failure-path tests remain applicable.
- [x] I updated docs for user-facing changes.
- [x] I verified no sensitive data was added.

## Notes for Reviewers

Merge this pull request with a normal merge commit. Do not squash or rebase it:
the immutable snapshot embeds the qualified commit, and release qualification
requires that exact commit to remain an ancestor of the final tag. If `main`
changes an implementation, test, canonical compatibility, release-script, or
release-workflow qualification path before merge, the snapshot must be rebuilt
on that updated base first.

The original tag triggered no package or GitHub Release publication because all
publish jobs were skipped after qualification failed. After this pull request
is merged and validated, the existing lightweight `v4.1.0` tag must be moved to
the merge commit and the new Release workflow run must be monitored separately
from the failed run.
