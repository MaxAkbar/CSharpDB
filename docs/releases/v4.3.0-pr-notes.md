## Summary

Prepare version4.3.0 for review. This release adds a first-party, review-first workflow for moving schemas and rows from external data sources into a new staged CSharpDB database.

CSV, JSON/NDJSON, SQLite, LiteDB 5, and the bounded MySQL 8.0/8.4 subset can be captured into retained, digest-pinned source packages, reviewed through deterministic plans and exact target DDL, applied or resumed using target-authoritative receipts, and checksum-validated before activation. Unsupported source objects remain visible as blocking diagnostics or explicit reviewed exclusions.

The release also implements a bounded on-premises SQL Server 2019/2022/2025 schema-and-row capture lane. SQL Server remains evaluation-only in v4.3.0: the analyzer cannot yet prove complete metadata visibility for the documented restricted account, so its non-overrideable diagnostic blocks planning and apply. Reviewers and operators must not grant `sysadmin` to bypass that safety boundary.

SQL Server and MySQL access run in fixed isolated worker processes. Credentials are accepted only through named environment variables and are needed only during capture. Retained packages are plaintext-sensitive and bind their catalog, provider contract, schema, rows, deterministic ordering, and package limits to an independently recorded SHA-256 manifest digest.

The branch also adds framework-dependent .NET 10 migration CLI archives for Windows x64, Linux x64, and macOS Apple silicon. The combined publisher preserves both fixed adapters and their notices/licenses, verifies byte-identical base CLI roots before composition, enforces safe output containment, writes checksums alongside the archives, and assigns exact executable modes to POSIX archives. User-directory installers refuse overwrites by default, preserve unrelated files under explicit force, reject linked path escapes, do not request elevation, and do not change `PATH`.

Public documentation now includes an end-to-end database migration guide with installation, source recipes, plan/preview/apply/resume/validate commands, recovery authority, security requirements, troubleshooting, and explicit provider qualification limits. It is separate from the older `CSharpDB.Core` to `CSharpDB.Primitives` library-rename guide.

## Type of Change

- [ ] Bug fix
- [x] New feature
- [ ] Breaking change
- [x] Documentation update
- [x] Refactor / maintenance
- [ ] Tests only

## Related Issues

None linked.

## Testing

Validation completed for the version4.3.0 migration branch:

- [x] `dotnet build CSharpDB.slnx`
- [x] Relevant tests executed
- [x] Failure-path tests executed (if applicable: cancellation, invalid/unsupported inputs, non-`DbException` paths)
- [x] Manual verification performed (if applicable)

Latest release-preparation validation:

- Release solution build passed with 0 warnings and 0 errors.
- The complete CLI test project passed: **383 passed, 0 failed**.
- Focused retained MySQL provider coverage passed: **206 passed, 0 failed**.
- Combined SQL Server/MySQL provider-runner coverage passed: **60 passed, 0 failed**.
- SQL Server worker coverage passed: **50 passed, 0 failed**.
- SQL Server and MySQL process-isolation checks passed on Windows; the corresponding Linux isolation checks passed under WSL Ubuntu.
- The combined publisher produced the `win-x64` zip, `linux-x64` tarball, `osx-arm64` tarball, and sibling `SHA256SUMS.txt`; all three archive digests were independently recomputed successfully.
- Both POSIX archives contained 137 supported entries with zero mode mismatches. Direct-run launchers and the POSIX installer were `0755`, directories were `0755`, and all other regular files were `0644`.
- A real Ubuntu extraction and installation smoke test directly executed the CLI before and after installation, preserved an unrelated destination file under `--force`, rejected a nested destination symlink, and left the outside sentinel unchanged.
- Packaging and installer regression tests passed on Windows, including linked-ancestor rejection and a static POSIX filesystem-root refusal contract. The preceding seven-test packaging suite also passed under Ubuntu.
- The migration documentation checks passed. All 119 local links/assets and 41 fragments on the modified migration pages resolve, and the sitemap is valid XML.
- PowerShell parser checks, POSIX shell syntax validation, scoped formatting, staged-diff whitespace checks, and credential-pattern scanning passed.

## Checklist

- [x] I followed the project style and conventions.
- [x] I added or updated tests for behavior changes.
- [x] I covered both success and failure paths for changed behavior.
- [x] I updated docs for user-facing changes.
- [x] I verified no sensitive data was added.

## Notes for Reviewers

- Review the migration catalog diagnostics, exclusions, rendered DDL, and isolated scratch-schema result as one approval packet before applying a plan.
- CSV, JSON/NDJSON, SQLite, LiteDB, and MySQL are apply-ready only for their documented bounded subsets. Unsupported shapes fail closed or require explicit, specific review.
- SQL Server is an evaluation capture lane in v4.3.0. A safe restricted account can capture candidate evidence, but metadata completeness remains `Unknown` and blocks plan/apply. Do not elevate the account to `sysadmin` to bypass the guard.
- Target receipts are the resume authority. Workspace files and run reports are evidence, not authoritative checkpoints.
- Retained source packages may contain plaintext-sensitive rows. Protect them like the source database and store the expected package digest in an independently trusted record.
- The release archives are framework-dependent and require the Microsoft .NET 10 runtime. Preserve the fixed `adapters/sqlserver` and `adapters/mysql` layout and all third-party notices/licenses.
- Broad live SQL Server/MySQL server, authentication, TLS, restricted-account, and published-runtime qualification remains deferred. The disposable Windows VM matrix was intentionally deferred for a later pass.
- The macOS Apple-silicon archive was built and its contents, checksums, types, and executable modes were validated, but it was not executed on native macOS hardware.

The branch contains:

- `b121471` — qualify the CSV, JSON/NDJSON, and SQLite migration MVP.
- `337e012` — add retained LiteDB migration, recovery, and validation.
- `e82f788` — add retained SQL Server schema and row capture.
- `0f6a978` — add retained MySQL migration and shared capture-path safety.
- `dbcf4df` — package and document the migration tooling.
- This version4.3.0 PR-note addition.
