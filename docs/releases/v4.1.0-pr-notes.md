## Summary

Prepare version4.1.0 from the SQL compatibility and CI-stabilization work on top of v4.0.4. This change establishes the generated, proof-backed compatibility matrix; expands defaults, checks, logical and composite keys, foreign keys, ALTER operations, `UNION ALL`, first-tier windows, metadata, EF-generated SQL, and transport/tooling coverage; and hardens dependency handling, transaction rollback, projection correctness, read-only EXPLAIN routing, pager shutdown, and release qualification.

## Type of Change

- [x] Bug fix
- [x] New feature
- [ ] Breaking change
- [x] Documentation update
- [x] Refactor / maintenance
- [ ] Tests only

## Related Issues

None.

## Testing

Validation completed with a zero-warning solution build, 2,306 passing tests across the solution, 43 documented compatibility features mapped to 141 proof IDs, documentation generation and link checks, positive plus tamper-rejection release snapshot fixtures, 40 executions of the two implicated daemon migration tests, and 330 complete daemon-suite test executions across fresh processes.

- [x] `dotnet build CSharpDB.slnx`
- [x] Relevant tests executed
- [x] Failure-path tests executed (if applicable: cancellation, invalid/unsupported inputs, non-`DbException` paths)
- [x] Manual verification performed (if applicable)

## Checklist

- [x] I followed the project style and conventions.
- [x] I added or updated tests for behavior changes.
- [x] I covered both success and failure paths for changed behavior.
- [x] I updated docs for user-facing changes.
- [x] I verified no sensitive data was added.

## Notes for Reviewers

Please review the compatibility matrix and roadmap claims together with their proof IDs. Pager disposal now latches background-checkpoint shutdown before transaction rollback and drains the captured task before final WAL cleanup. The first window tier is in-memory, while broader ALTER type/collation/key rewrites, spill-backed windows, physical-plan descriptors and `EXPLAIN ANALYZE`, and the remaining release-qualification lanes are intentionally retained as follow-up work.

The branch is based on v4.0.4 and contains only:

- `28c3181` — SQL compatibility foundations.
- `ab43e74` — execution hardening and release gates.
- `a41c737` — version4.1.0 release notes.
- `ff4ec65` — version4.1.0 release documentation.
- `36ab829` — checkpoint shutdown and cancellation-test stabilization.
- This PR-note refresh.
